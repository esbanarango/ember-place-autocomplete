{{#if hasBlock}}
  {{yield this}}
{{else}}
  {{input
    value=value
    classBinding="inputClass"
    name=name
    tabindex=tabindex
    disabled=disabled
    focus-out=(action "focusOut")
    placeholder=placeholder
    bubbles=true
  }}
{{/if}}
